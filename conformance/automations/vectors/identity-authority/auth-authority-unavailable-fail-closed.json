{
  "vectorId": "authority.unavailable-fail-closed",
  "vectorVersion": 1,
  "profile": "runtime-authority",
  "category": "identity-authority",
  "title": "Authority service unavailable: dispatch refuses fail-closed instead of guessing",
  "deterministic": true,
  "artifacts": ["coven.automations.authority.v1"],
  "specRefs": ["coven#857"],
  "virtualTime": { "start": "2026-03-01T00:00:00.000Z" },
  "input": {
    "definitions": [
      {
        "schemaVersion": 1, "id": "daily", "name": "Daily", "status": "ACTIVE",
        "rrule": "FREQ=DAILY;BYHOUR=9", "timezone": "utc", "misfire": "latest", "overlap": "forbid",
        "timeoutMinutes": 30, "runtime": "coven-code", "cwd": "work/proj", "prompt": "Run the routine."
      }
    ],
    "bindings": [
      {
        "automationId": "daily",
        "principalId": "principal-1",
        "familiarId": "charm",
        "familiarRevision": 1,
        "runtime": "coven-code",
        "requiredCapabilities": ["session.create"]
      }
    ],
    "operations": [
      { "op": "setAuthority", "available": false },
      { "op": "tick", "at": "2026-03-01T10:00:00.000Z" }
    ]
  },
  "expected": {
    "occurrences": [
      { "automationId": "daily", "state": "failed", "failureReasonMatches": "authority unavailable" }
    ],
    "dispatchCount": 0,
    "doctor": { "findingCodes": ["authority.unresolved-refusal"] }
  }
}
