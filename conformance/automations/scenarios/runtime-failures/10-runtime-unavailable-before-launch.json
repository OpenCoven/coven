{
  "vectorId": "golden.10-runtime-unavailable-before-launch",
  "vectorVersion": 1,
  "profile": "scheduler-reliability",
  "category": "scenario",
  "title": "Golden 10: runtime unavailable before launch fails the occurrence with a recorded reason and no silent loss",
  "deterministic": true,
  "artifacts": ["coven.automations.run.v1"],
  "specRefs": ["coven#856", "coven#858"],
  "virtualTime": { "start": "2026-03-01T00:00:00.000Z" },
  "input": {
    "definitions": [
      {
        "schemaVersion": 1, "id": "routine", "name": "Routine", "status": "ACTIVE",
        "rrule": "FREQ=DAILY;BYHOUR=9", "timezone": "utc", "misfire": "latest", "overlap": "forbid",
        "timeoutMinutes": 30, "runtime": "coven-code", "cwd": "work/proj", "prompt": "Run the routine."
      }
    ],
    "operations": [
      { "op": "setRetryPolicy", "maxAttempts": 1, "backoffSeconds": 0 },
      { "op": "runtimeBehavior", "behavior": "unavailable" },
      { "op": "tick", "at": "2026-03-01T10:00:00.000Z" }
    ]
  },
  "expected": {
    "occurrences": [
      { "automationId": "routine", "state": "failed", "failureReasonMatches": "runtime unavailable" }
    ],
    "runs": [{ "automationId": "routine", "status": "failed" }],
    "dispatchCount": 1,
    "invariants": ["no-silent-eligible-occurrence-loss", "no-false-success-under-injected-failure"]
  }
}
