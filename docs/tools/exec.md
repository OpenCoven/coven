---
summary: "Run shell commands inside a session's project root."
read_when:
  - Calling shell commands from a familiar
title: "exec"
description: "The exec tool surface for OpenCoven familiars: how agents run shell commands inside a harness session under the daemon's safety model."
---

Stub — fill in.
