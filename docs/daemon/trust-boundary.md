---
summary: "Clients can ask; only the daemon decides."
read_when:
  - Documenting trust for a security review
title: "Trust boundary"
description: "Trust boundary for the Coven daemon: filesystem permissions on the Unix socket are the only auth check, so same-user local access is the trust surface."
---

Stub — fill in. See [Daemon overview](/daemon/index).
