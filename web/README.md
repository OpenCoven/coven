# OpenCoven landing page

Static landing page for OpenCoven.

Preview locally:

```sh
python3 -m http.server 4173 --directory web
```

Then open <http://localhost:4173>.

The page is dependency-free so it can be deployed to any static host.
